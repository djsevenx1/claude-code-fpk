appname=claude-code-sandbox
version=1.2.6
display_name=Claude Code
desc=<p>把 Anthropic Claude Code CLI 装到飞牛 NAS 上，以<b>包用户 + 工作目录隔离</b>方式沙箱运行，通过 Web 终端 (ttyd) 访问。</p><p>依赖 fnOS 应用商店的 <code>nodejs_v22</code> 运行时。</p>
source=thirdparty
platform=x86
maintainer=djsevenx1
maintainer_url=https://github.com/djsevenx1
os_min_version=0.9.27
desktop_uidir=ui
desktop_applaunchname=claude-code-sandbox.main
service_port=7681
checkport=true
install_type=root
install_dep_apps=nodejs_v22
ctl_stop=true
disable_authorization_path=false
changelog=<ul><li>1.2.6 照 OpenClaw (oc-deploy v1.2.3) 写法重写：新建 <code>cmd/lib/log.sh</code> 统一日志到 <code>${TRIM_PKGVAR}/info.log</code>；<code>install_init</code>/<code>install_callback</code>/<code>main</code>/<code>uninstall_init</code>/<code>upgrade_*</code>/<code>config_*</code> 全部 source 共享 log；<code>diag_to_user</code> 失败时把"关键路径探测 + node 版本 + 最近 50 行日志 + 自查步骤"打包写到 <code>TRIM_TEMP_LOGFILE</code> 给用户看；<code>install_callback</code> 加幂等：重装时 .env/settings.json/.claude.json 已存在则保留（不再被空字符串覆盖 API Key）；<code>main</code> 和 <code>uninstall_init</code> 加 <code>pkill -f ttyd</code> 兜底清理（PID 文件丢了也能清残留）</li><li>1.2.5 图标换成 Anthropic Claude Code 官方 4 角 sparkle（coral #D97757 on 深蓝 #18182F）</li></ul>
